import React, { useState, useEffect, useRef } from 'react';
import { 
  Monitor, Layout, Layers, ToggleLeft, ToggleRight, 
  AlertTriangle, Megaphone, Power, CheckCircle, Smartphone, Database, Server, HardDrive, Copy, Check, Save, RefreshCw, Key,
  Globe, MapPin, Phone, Mail, Upload, X, Image as ImageIcon
} from 'lucide-react';
import { useSiteConfig, ToggableModule, LandingSection } from '../../../contexts/SiteConfigContext';
import { isSupabaseConfigured } from '../../../services/supabaseClient';
import { AppModule } from '../../../types';

const SCHEMA_SQL = `
-- 1. JOBS TABLE
create table if not exists public.jobs (
  id bigint generated by default as identity primary key,
  created_at timestamp with time zone default timezone('utc'::text, now()) not null
);
-- Add columns if they don't exist (Lowercase for consistency)
alter table public.jobs add column if not exists title text;
alter table public.jobs add column if not exists company text;
alter table public.jobs add column if not exists location text;
alter table public.jobs add column if not exists salary text;
alter table public.jobs add column if not exists type text;
alter table public.jobs add column if not exists category text;
alter table public.jobs add column if not exists description text;
alter table public.jobs add column if not exists deadline text;
alter table public.jobs add column if not exists postedby text;
alter table public.jobs add column if not exists posteddate text;
alter table public.jobs add column if not exists status text default 'Active';
alter table public.jobs add column if not exists views numeric default 0;
alter table public.jobs add column if not exists level text;

-- Policy
alter table public.jobs enable row level security;
drop policy if exists "Allow all" on public.jobs;
create policy "Allow all" on public.jobs for all using (true) with check (true);

-- 2. BLOGS TABLE
create table if not exists public.blogs (
  id bigint generated by default as identity primary key,
  created_at timestamp with time zone default timezone('utc'::text, now()) not null
);
alter table public.blogs add column if not exists title text;
alter table public.blogs add column if not exists category text;
alter table public.blogs add column if not exists content text;
alter table public.blogs add column if not exists author text;
alter table public.blogs add column if not exists image text;
alter table public.blogs add column if not exists posteddate text;
alter table public.blogs add column if not exists date text;
alter table public.blogs add column if not exists readtime text;
alter table public.blogs add column if not exists views numeric default 0;
alter table public.blogs add column if not exists status text default 'Active';
alter table public.blogs add column if not exists excerpt text;

alter table public.blogs enable row level security;
drop policy if exists "Allow all" on public.blogs;
create policy "Allow all" on public.blogs for all using (true) with check (true);

-- 3. REQUESTS TABLE (Combined fields for Jobs & Blogs)
create table if not exists public.requests (
  id bigint generated by default as identity primary key,
  created_at timestamp with time zone default timezone('utc'::text, now()) not null
);
alter table public.requests add column if not exists contenttype text;
alter table public.requests add column if not exists title text;
alter table public.requests add column if not exists category text;
alter table public.requests add column if not exists content text;
alter table public.requests add column if not exists description text;
alter table public.requests add column if not exists company text;
alter table public.requests add column if not exists location text;
alter table public.requests add column if not exists salary text;
alter table public.requests add column if not exists type text;
alter table public.requests add column if not exists deadline text;
alter table public.requests add column if not exists author text;
alter table public.requests add column if not exists postedby text;
alter table public.requests add column if not exists posteddate text;
alter table public.requests add column if not exists image text;
alter table public.requests add column if not exists status text default 'Pending';
alter table public.requests add column if not exists level text;

alter table public.requests enable row level security;
drop policy if exists "Allow all" on public.requests;
create policy "Allow all" on public.requests for all using (true) with check (true);

-- 4. MARKET PRICES
create table if not exists public.market_prices (
  id bigint generated by default as identity primary key,
  created_at timestamp with time zone default timezone('utc'::text, now()) not null,
  nameen text,
  namebn text,
  unit text,
  today numeric,
  yesterday numeric,
  trend text
);
alter table public.market_prices enable row level security;
drop policy if exists "Allow all" on public.market_prices;
create policy "Allow all" on public.market_prices for all using (true) with check (true);

-- 5. USERS
create table if not exists public.users (
  id text primary key,
  created_at timestamp with time zone default timezone('utc'::text, now()) not null,
  name text,
  email text,
  role text,
  phone text,
  location text,
  avatar text,
  status text default 'Active',
  date text,
  password text
);
alter table public.users enable row level security;
drop policy if exists "Allow all" on public.users;
create policy "Allow all" on public.users for all using (true) with check (true);

-- 6. OTHERS
create table if not exists public.grievances (id bigint generated by default as identity primary key, created_at timestamp with time zone default timezone('utc'::text, now()) not null, location text, issue text, type text, image text, "user" text, status text default 'Pending', date text, severity text);
alter table public.grievances enable row level security; drop policy if exists "Allow all" on public.grievances; create policy "Allow all" on public.grievances for all using (true) with check (true);

create table if not exists public.wholesale_ads (id bigint generated by default as identity primary key, created_at timestamp with time zone default timezone('utc'::text, now()) not null, product text, quantity text, price text, seller text, sellertype text, location text, date text, status text default 'Pending');
alter table public.wholesale_ads enable row level security; drop policy if exists "Allow all" on public.wholesale_ads; create policy "Allow all" on public.wholesale_ads for all using (true) with check (true);

create table if not exists public.retail_products (id bigint generated by default as identity primary key, created_at timestamp with time zone default timezone('utc'::text, now()) not null, nameen text, namebn text, price numeric, unit text, img text, category text, stock text default 'Available');
alter table public.retail_products enable row level security; drop policy if exists "Allow all" on public.retail_products; create policy "Allow all" on public.retail_products for all using (true) with check (true);

create table if not exists public.lawyers (id bigint generated by default as identity primary key, created_at timestamp with time zone default timezone('utc'::text, now()) not null, name text, speciality text, phone text, location text, status text default 'Active');
alter table public.lawyers enable row level security; drop policy if exists "Allow all" on public.lawyers; create policy "Allow all" on public.lawyers for all using (true) with check (true);

create table if not exists public.exchange_rates (id bigint generated by default as identity primary key, created_at timestamp with time zone default timezone('utc'::text, now()) not null, currency text, rate numeric, trend text);
alter table public.exchange_rates enable row level security; drop policy if exists "Allow all" on public.exchange_rates; create policy "Allow all" on public.exchange_rates for all using (true) with check (true);

create table if not exists public.vocational_courses (id bigint generated by default as identity primary key, created_at timestamp with time zone default timezone('utc'::text, now()) not null, title text, titlebn text, category text, duration text, fee numeric, image text, status text default 'Active');
alter table public.vocational_courses enable row level security; drop policy if exists "Allow all" on public.vocational_courses; create policy "Allow all" on public.vocational_courses for all using (true) with check (true);

create table if not exists public.donors (id bigint generated by default as identity primary key, created_at timestamp with time zone default timezone('utc'::text, now()) not null, name text, phone text, district text, "group" text, lastdonation text);
alter table public.donors enable row level security; drop policy if exists "Allow all" on public.donors; create policy "Allow all" on public.donors for all using (true) with check (true);

create table if not exists public.enrolled_courses (id bigint generated by default as identity primary key, created_at timestamp with time zone default timezone('utc'::text, now()) not null, title text, titlebn text, enrolleddate text, progress numeric, status text);
alter table public.enrolled_courses enable row level security; drop policy if exists "Allow all" on public.enrolled_courses; create policy "Allow all" on public.enrolled_courses for all using (true) with check (true);

-- REALTIME
alter publication supabase_realtime set table 
  public.jobs, public.blogs, public.market_prices, public.users, public.requests, 
  public.grievances, public.wholesale_ads, public.retail_products, public.lawyers, 
  public.exchange_rates, public.vocational_courses, public.donors, public.enrolled_courses;
`;

export const AdminWebsiteManage = () => {
  const { modules, sections, settings, toggleModule, toggleSection, updateSettings } = useSiteConfig();
  const [copied, setCopied] = useState(false);
  const logoInputRef = useRef<HTMLInputElement>(null);

  // DB Config State
  const [dbUrl, setDbUrl] = useState(localStorage.getItem('dream_sb_url') || '');
  const [dbKey, setDbKey] = useState(localStorage.getItem('dream_sb_key') || '');
  const [isSaving, setIsSaving] = useState(false);

  const handleCopySQL = () => {
    navigator.clipboard.writeText(SCHEMA_SQL);
    setCopied(true);
    setTimeout(() => setCopied(false), 2000);
  };

  const handleSaveDbConfig = () => {
    setIsSaving(true);
    localStorage.setItem('dream_sb_url', dbUrl.trim());
    localStorage.setItem('dream_sb_key', dbKey.trim());
    
    setTimeout(() => {
        setIsSaving(false);
        alert('Configuration Saved! The page will refresh to connect.');
        window.location.reload();
    }, 1000);
  };

  const handleDisconnect = () => {
      if(confirm('Are you sure? This will disconnect the database and revert to local storage.')) {
          localStorage.removeItem('dream_sb_url');
          localStorage.removeItem('dream_sb_key');
          window.location.reload();
      }
  }

  const handleLogoUpload = (e: React.ChangeEvent<HTMLInputElement>) => {
    const file = e.target.files?.[0];
    if (file) {
      const reader = new FileReader();
      reader.onloadend = () => {
        updateSettings('websiteLogo', reader.result as string);
      };
      reader.readAsDataURL(file);
    }
  };

  const handleRemoveLogo = () => {
    updateSettings('websiteLogo', '');
    if (logoInputRef.current) logoInputRef.current.value = '';
  };

  // Helper for Toggle Switch
  const ToggleSwitch = ({ label, checked, onChange, color = 'bg-green-500' }: { label: string, checked: boolean, onChange: () => void, color?: string }) => (
    <div className="flex items-center justify-between p-4 bg-gray-50 rounded-xl border border-gray-100 hover:border-gray-300 transition-all">
      <span className="font-medium text-gray-900">{label}</span>
      <button 
        onClick={onChange}
        className={`relative w-12 h-6 rounded-full transition-colors duration-300 ${checked ? color : 'bg-gray-300'}`}
      >
        <div className={`absolute top-1 left-1 w-4 h-4 bg-white rounded-full shadow-md transform transition-transform duration-300 ${checked ? 'translate-x-6' : 'translate-x-0'}`}></div>
      </button>
    </div>
  );

  return (
    <div className="space-y-8 animate-fade-in">
      
      {/* Database Connection Panel */}
      <div className="bg-white rounded-2xl shadow-sm border border-gray-200 overflow-hidden">
         <div className={`p-6 border-b flex items-center justify-between ${isSupabaseConfigured ? 'bg-green-50 border-green-200' : 'bg-amber-50 border-amber-200'}`}>
            <div className="flex items-center gap-3">
                <div className={`p-2 rounded-lg ${isSupabaseConfigured ? 'bg-green-100 text-green-700' : 'bg-amber-100 text-amber-700'}`}>
                    <Database size={24} />
                </div>
                <div>
                    <h3 className={`text-lg font-bold ${isSupabaseConfigured ? 'text-green-900' : 'text-amber-900'}`}>
                        {isSupabaseConfigured ? 'Database Connected (Online)' : 'Database Not Connected (Offline Mode)'}
                    </h3>
                    <p className={`text-xs ${isSupabaseConfigured ? 'text-green-700' : 'text-amber-700'}`}>
                        {isSupabaseConfigured 
                        ? 'Data is syncing with Supabase cloud.' 
                        : 'Currently using browser storage. Other users cannot see updates.'}
                    </p>
                </div>
            </div>
            {isSupabaseConfigured && (
                <button onClick={handleDisconnect} className="text-xs text-red-600 underline hover:text-red-800">Disconnect</button>
            )}
         </div>
         
         <div className="p-6">
            {!isSupabaseConfigured ? (
                <div className="space-y-4">
                    <div className="bg-blue-50 p-4 rounded-xl border border-blue-100 text-sm text-blue-800 mb-4">
                        <strong>Instructions:</strong>
                        <ol className="list-decimal ml-5 mt-2 space-y-1">
                            <li>Go to <a href="https://supabase.com" target="_blank" className="underline font-bold">Supabase.com</a> and create a project.</li>
                            <li>Go to Project Settings - API.</li>
                            <li>Copy the <strong>Project URL</strong> and <strong>anon public key</strong> below.</li>
                        </ol>
                    </div>
                    <div>
                        <label className="block text-sm font-bold text-gray-700 mb-1">Project URL</label>
                        <input 
                            type="text" 
                            value={dbUrl}
                            onChange={(e) => setDbUrl(e.target.value)}
                            placeholder="https://your-project.supabase.co"
                            className="w-full p-3 border border-gray-300 rounded-xl focus:ring-2 focus:ring-blue-500 outline-none"
                        />
                    </div>
                    <div>
                        <label className="block text-sm font-bold text-gray-700 mb-1">API Key (anon / public)</label>
                        <div className="relative">
                            <input 
                                type="password" 
                                value={dbKey}
                                onChange={(e) => setDbKey(e.target.value)}
                                placeholder="eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
                                className="w-full p-3 border border-gray-300 rounded-xl focus:ring-2 focus:ring-blue-500 outline-none pr-10"
                            />
                            <Key size={16} className="absolute right-4 top-4 text-gray-400"/>
                        </div>
                    </div>
                    <button 
                        onClick={handleSaveDbConfig}
                        disabled={isSaving || !dbUrl || !dbKey}
                        className="w-full py-3 bg-blue-600 hover:bg-blue-700 text-white font-bold rounded-xl shadow-lg shadow-blue-200 flex items-center justify-center gap-2"
                    >
                        {isSaving ? <RefreshCw className="animate-spin" /> : <Save size={18} />}
                        {isSaving ? 'Connecting...' : 'Connect to Database'}
                    </button>
                </div>
            ) : (
                <div className="text-center py-6">
                    <div className="w-16 h-16 bg-green-100 text-green-600 rounded-full flex items-center justify-center mx-auto mb-3">
                        <CheckCircle size={32} />
                    </div>
                    <h3 className="font-bold text-gray-900 text-lg">System is Online</h3>
                    <p className="text-gray-500 mb-6">Run this SQL code to fix database columns and permissions.</p>
                    
                    <div className="bg-gray-900 text-white p-6 rounded-2xl border border-gray-800 text-left">
                        <h4 className="font-bold text-lg mb-2 flex items-center gap-2 text-green-400"><Server size={20}/> Database Repair & Setup</h4>
                        <p className="text-sm text-gray-300 mb-4 leading-relaxed">
                            Run this SQL in Supabase Query Editor. It uses <strong>IF NOT EXISTS</strong> to safely add missing columns without deleting data. It fixes permission issues and normalizes columns to lowercase.
                        </p>
                        
                        <div className="bg-black/50 p-4 rounded-xl font-mono text-xs text-green-300 mb-4 h-32 overflow-y-auto border border-gray-700">
                            {SCHEMA_SQL}
                        </div>

                        <button 
                            onClick={handleCopySQL}
                            className="w-full flex items-center justify-center gap-2 px-6 py-3 bg-green-600 hover:bg-green-700 text-white rounded-xl text-sm font-bold transition-all shadow-lg shadow-green-900/20"
                        >
                            {copied ? <Check size={18} /> : <Copy size={18} />}
                            {copied ? 'SQL Copied!' : 'Copy SQL Code'}
                        </button>
                        <p className="text-center text-xs text-gray-500 mt-2">Go to Supabase {'>'} SQL Editor {'>'} New Query {'>'} Paste & Run</p>
                    </div>
                </div>
            )}
         </div>
      </div>

      {/* General Information */}
      <div className="bg-white rounded-2xl shadow-sm border border-gray-200 overflow-hidden">
        <div className="p-6 border-b border-gray-100 flex items-center gap-3 bg-gray-50">
          <div className="p-2 bg-blue-100 text-blue-700 rounded-lg">
            <Globe size={20} />
          </div>
          <div>
            <h3 className="text-lg font-bold text-gray-900">General Information</h3>
            <p className="text-xs text-gray-500">Website branding, logo, and contact details</p>
          </div>
        </div>
        
        <div className="p-6 grid grid-cols-1 lg:grid-cols-3 gap-8">
           {/* Logo Section */}
           <div className="col-span-1">
              <label className="block text-sm font-bold text-gray-700 mb-3">Website Logo</label>
              <div 
                className="border-2 border-dashed border-gray-300 rounded-xl p-6 flex flex-col items-center justify-center min-h-[200px] cursor-pointer hover:bg-gray-50 transition-colors relative group"
                onClick={() => logoInputRef.current?.click()}
              >
                 <input 
                   type="file" 
                   accept="image/*" 
                   className="hidden" 
                   ref={logoInputRef}
                   onChange={handleLogoUpload}
                 />
                 
                 {settings.websiteLogo ? (
                   <div className="relative w-full h-full flex flex-col items-center">
                      <img src={settings.websiteLogo} alt="Logo" className="max-h-32 object-contain mb-2" />
                      <button 
                        onClick={(e) => { e.stopPropagation(); handleRemoveLogo(); }}
                        className="absolute top-0 right-0 bg-red-500 text-white p-1 rounded-full opacity-0 group-hover:opacity-100 transition-opacity"
                      >
                        <X size={16} />
                      </button>
                      <p className="text-xs text-gray-500 mt-2">Click to change</p>
                   </div>
                 ) : (
                   <div className="text-center">
                      <div className="bg-gray-100 p-3 rounded-full inline-block mb-3">
                        <ImageIcon size={24} className="text-gray-400" />
                      </div>
                      <p className="text-sm font-bold text-gray-600">Upload Logo</p>
                      <p className="text-xs text-gray-400 mt-1">PNG, JPG, SVG (Max 2MB)</p>
                   </div>
                 )}
              </div>
           </div>

           {/* Info Inputs */}
           <div className="col-span-1 lg:col-span-2 space-y-5">
              <div>
                 <label className="block text-sm font-bold text-gray-700 mb-2">Website Title</label>
                 <input 
                   type="text" 
                   value={settings.websiteTitle}
                   onChange={(e) => updateSettings('websiteTitle', e.target.value)}
                   className="w-full px-4 py-3 border border-gray-300 rounded-xl focus:ring-2 focus:ring-blue-500 outline-none"
                   placeholder="e.g. Dream BD"
                 />
              </div>
              
              <div className="grid grid-cols-1 md:grid-cols-2 gap-5">
                 <div>
                    <label className="block text-sm font-bold text-gray-700 mb-2 flex items-center gap-2">
                      <Mail size={16} className="text-gray-400"/> Contact Email
                    </label>
                    <input 
                      type="text" 
                      value={settings.contactEmail}
                      onChange={(e) => updateSettings('contactEmail', e.target.value)}
                      className="w-full px-4 py-3 border border-gray-300 rounded-xl focus:ring-2 focus:ring-blue-500 outline-none"
                      placeholder="info@example.com"
                    />
                 </div>
                 <div>
                    <label className="block text-sm font-bold text-gray-700 mb-2 flex items-center gap-2">
                      <Phone size={16} className="text-gray-400"/> Contact Phone
                    </label>
                    <input 
                      type="text" 
                      value={settings.contactPhone}
                      onChange={(e) => updateSettings('contactPhone', e.target.value)}
                      className="w-full px-4 py-3 border border-gray-300 rounded-xl focus:ring-2 focus:ring-blue-500 outline-none"
                      placeholder="+880 1..."
                    />
                 </div>
              </div>

              <div>
                 <label className="block text-sm font-bold text-gray-700 mb-2 flex items-center gap-2">
                   <MapPin size={16} className="text-gray-400"/> Address
                 </label>
                 <textarea 
                   rows={2}
                   value={settings.address}
                   onChange={(e) => updateSettings('address', e.target.value)}
                   className="w-full px-4 py-3 border border-gray-300 rounded-xl focus:ring-2 focus:ring-blue-500 outline-none resize-none"
                   placeholder="Office Address..."
                 />
              </div>
           </div>
        </div>
      </div>

      {/* Advanced Settings */}
      <div className="bg-white rounded-2xl shadow-sm border border-gray-200 overflow-hidden">
        <div className="p-6 border-b border-gray-100 flex items-center gap-3 bg-gray-50">
          <div className="p-2 bg-purple-100 text-purple-700 rounded-lg">
            <Monitor size={20} />
          </div>
          <div>
            <h3 className="text-lg font-bold text-gray-900">System Controls</h3>
            <p className="text-xs text-gray-500">Global settings and maintenance</p>
          </div>
        </div>
        <div className="p-6 grid grid-cols-1 md:grid-cols-2 gap-8">
           {/* Maintenance Mode */}
           <div className={`p-6 rounded-2xl border-2 transition-all ${settings.maintenanceMode ? 'border-red-500 bg-red-50' : 'border-gray-200 bg-white'}`}>
              <div className="flex justify-between items-start mb-4">
                 <div className="flex items-center gap-3">
                    <div className={`p-2 rounded-full ${settings.maintenanceMode ? 'bg-red-200 text-red-700' : 'bg-gray-100 text-gray-500'}`}>
                       <Power size={24} />
                    </div>
                    <div>
                       <h4 className="font-bold text-gray-900">Maintenance Mode</h4>
                       <p className="text-xs text-gray-500">Take site offline for users</p>
                    </div>
                 </div>
                 <button 
                    onClick={() => updateSettings('maintenanceMode', !settings.maintenanceMode)}
                    className={`font-bold text-xs px-3 py-1 rounded-full ${settings.maintenanceMode ? 'bg-red-600 text-white' : 'bg-gray-200 text-gray-700'}`}
                 >
                    {settings.maintenanceMode ? 'ENABLED' : 'DISABLED'}
                 </button>
              </div>
              <p className="text-sm text-gray-600 leading-relaxed">
                When enabled, regular users will see a "Under Maintenance" screen. Admins can still access the dashboard.
              </p>
           </div>

           {/* Announcement Bar */}
           <div className="space-y-4">
              <div className="flex items-center justify-between">
                 <h4 className="font-bold text-gray-900 flex items-center gap-2"><Megaphone size={18} className="text-orange-500"/> Announcement Bar</h4>
                 <div className="flex items-center gap-2">
                    <span className="text-xs font-bold text-gray-500">{settings.announcementActive ? 'ON' : 'OFF'}</span>
                    <button 
                      onClick={() => updateSettings('announcementActive', !settings.announcementActive)}
                      className={`relative w-10 h-5 rounded-full transition-colors ${settings.announcementActive ? 'bg-orange-500' : 'bg-gray-300'}`}
                    >
                      <div className={`absolute top-1 left-1 w-3 h-3 bg-white rounded-full transition-transform ${settings.announcementActive ? 'translate-x-5' : 'translate-x-0'}`}></div>
                    </button>
                 </div>
              </div>
              <textarea 
                value={settings.announcement}
                onChange={(e) => updateSettings('announcement', e.target.value)}
                className="w-full p-3 bg-white border border-gray-300 rounded-xl text-sm text-gray-900 focus:ring-2 focus:ring-orange-500 focus:border-orange-500 placeholder-gray-400"
                rows={3}
                placeholder="Enter announcement text..."
              />
              <p className="text-xs text-gray-500">This text will appear at the very top of the website.</p>
           </div>
        </div>
      </div>

      <div className="grid grid-cols-1 lg:grid-cols-2 gap-8">
        {/* Header Modules Toggle */}
        <div className="bg-white rounded-2xl shadow-sm border border-gray-200">
          <div className="p-5 border-b border-gray-100 flex items-center gap-3">
             <Layers size={20} className="text-blue-600"/>
             <h3 className="font-bold text-gray-900">Header Modules</h3>
          </div>
          <div className="p-6">
             <div className="grid grid-cols-1 sm:grid-cols-2 gap-4">
                <ToggleSwitch label="Amar Bangladesh" checked={modules[AppModule.AMAR_BD]} onChange={() => toggleModule(AppModule.AMAR_BD)} color="bg-blue-600" />
                <ToggleSwitch label="Amar Jela" checked={modules[AppModule.AMAR_JELA]} onChange={() => toggleModule(AppModule.AMAR_JELA)} color="bg-blue-600" />
                <ToggleSwitch label="Bazar Sodai" checked={modules[AppModule.BAZAR_SODAI]} onChange={() => toggleModule(AppModule.BAZAR_SODAI)} color="bg-blue-600" />
                <ToggleSwitch label="Jobs" checked={modules[AppModule.JOB]} onChange={() => toggleModule(AppModule.JOB)} color="bg-blue-600" />
                <ToggleSwitch label="Blog" checked={modules[AppModule.BLOG]} onChange={() => toggleModule(AppModule.BLOG)} color="bg-blue-600" />
                <ToggleSwitch label="Contact" checked={modules[AppModule.CONTACT]} onChange={() => toggleModule(AppModule.CONTACT)} color="bg-blue-600" />
                
                {/* Services Dropdown Items */}
                <div className="sm:col-span-2 mt-4 mb-2">
                   <p className="text-xs font-bold text-gray-500 uppercase tracking-wider">Services Dropdown Items</p>
                </div>
                <ToggleSwitch label="Agriculture" checked={modules[AppModule.AGRI]} onChange={() => toggleModule(AppModule.AGRI)} color="bg-blue-600" />
                <ToggleSwitch label="Health" checked={modules[AppModule.HEALTH]} onChange={() => toggleModule(AppModule.HEALTH)} color="bg-blue-600" />
                <ToggleSwitch label="Education" checked={modules[AppModule.EDU]} onChange={() => toggleModule(AppModule.EDU)} color="bg-blue-600" />
                <ToggleSwitch label="Transport" checked={modules[AppModule.TRANSPORT]} onChange={() => toggleModule(AppModule.TRANSPORT)} color="bg-blue-600" />
                <ToggleSwitch label="Disaster" checked={modules[AppModule.DISASTER]} onChange={() => toggleModule(AppModule.DISASTER)} color="bg-blue-600" />
                <ToggleSwitch label="Fishery" checked={modules[AppModule.FISHERY]} onChange={() => toggleModule(AppModule.FISHERY)} color="bg-blue-600" />
                <ToggleSwitch label="Waste Mgmt" checked={modules[AppModule.WASTE]} onChange={() => toggleModule(AppModule.WASTE)} color="bg-blue-600" />
                <ToggleSwitch label="Crafts" checked={modules[AppModule.CRAFT]} onChange={() => toggleModule(AppModule.CRAFT)} color="bg-blue-600" />
             </div>
          </div>
        </div>

        {/* Landing Page Sections Toggle */}
        <div className="bg-white rounded-2xl shadow-sm border border-gray-200 h-fit">
          <div className="p-5 border-b border-gray-100 flex items-center gap-3">
             <Layout size={20} className="text-indigo-600"/>
             <h3 className="font-bold text-gray-900">Landing Page Sections</h3>
          </div>
          <div className="p-6">
             <div className="space-y-4">
                <ToggleSwitch label="Hero Section (Top Banner)" checked={sections.hero} onChange={() => toggleSection('hero')} color="bg-indigo-600" />
                <ToggleSwitch label="About Platform" checked={sections.about} onChange={() => toggleSection('about')} color="bg-indigo-600" />
                <ToggleSwitch label="How it Works" checked={sections.features} onChange={() => toggleSection('features')} color="bg-indigo-600" />
                <ToggleSwitch label="Beautiful Bangladesh Gallery" checked={sections.gallery} onChange={() => toggleSection('gallery')} color="bg-indigo-600" />
                <ToggleSwitch label="Testimonials" checked={sections.testimonials} onChange={() => toggleSection('testimonials')} color="bg-indigo-600" />
                
                <div className="mt-6 mb-2">
                   <p className="text-xs font-bold text-gray-500 uppercase tracking-wider">Module Previews on Home</p>
                </div>
                <div className="grid grid-cols-1 sm:grid-cols-2 gap-4">
                   <ToggleSwitch label="Craft Preview" checked={sections.craft} onChange={() => toggleSection('craft')} color="bg-indigo-600" />
                   <ToggleSwitch label="Agri Preview" checked={sections.agri} onChange={() => toggleSection('agri')} color="bg-indigo-600" />
                   <ToggleSwitch label="Health Preview" checked={sections.health} onChange={() => toggleSection('health')} color="bg-indigo-600" />
                   <ToggleSwitch label="Education Preview" checked={sections.edu} onChange={() => toggleSection('edu')} color="bg-indigo-600" />
                   <ToggleSwitch label="Transport Preview" checked={sections.transport} onChange={() => toggleSection('transport')} color="bg-indigo-600" />
                   <ToggleSwitch label="Waste Preview" checked={sections.waste} onChange={() => toggleSection('waste')} color="bg-indigo-600" />
                   <ToggleSwitch label="Fishery Preview" checked={sections.fishery} onChange={() => toggleSection('fishery')} color="bg-indigo-600" />
                   <ToggleSwitch label="Disaster Preview" checked={sections.disaster} onChange={() => toggleSection('disaster')} color="bg-indigo-600" />
                </div>
             </div>
          </div>
        </div>

      </div>
    </div>
  );
};